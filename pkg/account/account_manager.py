class AccountManager():
	def __init__(self):
		self.accounts = list()
	
	def __init__(self, accountsJsonPath)
		if accountsJsonPath is None:
			except Exception:
				raise Exception("path to accounts.json is None")
		
		self.accounts = __parse_accounts_json(accountsJsonPath)
		
